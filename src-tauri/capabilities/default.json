{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal capability set for Selah windows: core events, window and webview defaults. Native functionality is exposed through explicit Selah commands.",
  "windows": ["main", "presentation"],
  "permissions": ["core:default"]
}